# Backend base URL (used in production builds; dev server proxies /v1 to localhost:8080)
VITE_API_BASE_URL=

# Admin bearer token for read/write admin APIs.
# Set this to the value of HOMELYTICS_ADMIN_TOKEN when the backend is running with one.
# For normal email/password login this can be left empty.
VITE_ADMIN_TOKEN=
