# Backend base URL (used in production builds; dev server proxies /v1 to localhost:8080)
VITE_API_BASE_URL=

# Bearer token for admin read APIs; leave empty if HOMELYTICS_ADMIN_TOKEN is not set
VITE_ADMIN_TOKEN=
